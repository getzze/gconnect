# These are temporary files generated by different editors.  If your
# editor generates something else, please add a pattern for it here.
*~
*.#
*.bak

# It's common practice to create a build subdirectory to, well, build
# in.  Obviously we don't want the build directory in git, so we'll
# ignore it here.
/build

# These files are generated by CMake when you build.  Having these
# here helps keep the tree clean, even when sourcedir and builddir are
# the same.
/CMakeCache.txt
/CPackConfig.cmake
/CPackSourceConfig.cmake
/install_manifest.txt
/Testing
cmake_install.cmake
CTestTestfile.cmake
CMakeFiles/
Makefile
